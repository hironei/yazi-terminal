<Window x:Class="YaziDesktopHost.CommandPaletteWindow"
        x:ClassModifier="internal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Command Palette"
        Width="560"
        Height="340"
        MinWidth="440"
        MinHeight="240"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        ShowActivated="True"
        WindowStartupLocation="CenterOwner"
        AllowsTransparency="True"
        Background="Transparent"
        PreviewKeyDown="Window_PreviewKeyDown">
    <Border x:Name="PaletteSurface"
            Padding="14"
            Background="{DynamicResource PaletteBackgroundBrush}"
            BorderBrush="{DynamicResource PaletteBorderBrush}"
            BorderThickness="1"
            CornerRadius="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Text="Command Palette"
                       Foreground="{DynamicResource PaletteForegroundBrush}"
                       FontSize="14"
                       FontWeight="SemiBold"
                       Margin="2,0,0,10" />

            <TextBox x:Name="SearchBox"
                     Grid.Row="1"
                     Height="34"
                     Padding="9,5"
                     Background="{DynamicResource PaletteInputBackgroundBrush}"
                     BorderBrush="{DynamicResource PaletteBorderBrush}"
                     Foreground="{DynamicResource PaletteForegroundBrush}"
                     CaretBrush="{DynamicResource PaletteForegroundBrush}"
                     FontSize="14"
                     TextChanged="SearchBox_TextChanged" />

            <Grid Grid.Row="2" Margin="0,10,0,0">
                <ListBox x:Name="CommandList"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{DynamicResource PaletteForegroundBrush}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         MouseDoubleClick="CommandList_MouseDoubleClick">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="10,8" />
                            <Setter Property="Margin" Value="0,1" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Background" Value="Transparent" />
                            <Setter Property="Foreground" Value="{DynamicResource PaletteForegroundBrush}" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border Background="{TemplateBinding Background}"
                                                Padding="{TemplateBinding Padding}"
                                                Margin="{TemplateBinding Margin}">
                                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                              Content="{TemplateBinding Content}"
                                                              ContentTemplate="{TemplateBinding ContentTemplate}"
                                                              ContentStringFormat="{TemplateBinding ContentStringFormat}" />
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource PaletteSelectionBackgroundBrush}" />
                                    <Setter Property="Foreground" Value="{DynamicResource PaletteSelectionForegroundBrush}" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}"
                                           FontWeight="SemiBold"
                                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}" />
                                <TextBlock Text="{Binding Description}"
                                           FontSize="12"
                                           Opacity="0.8"
                                           Foreground="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=Foreground}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <TextBlock x:Name="EmptyText"
                           Text="No matching commands"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource PaletteForegroundBrush}"
                           Opacity="0.75"
                           Visibility="Collapsed" />
            </Grid>
        </Grid>
    </Border>
</Window>
